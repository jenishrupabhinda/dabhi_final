<?php
/**
 * EmailSender — SMTP email via PHPMailer (see composer.json).
 *
 * NEXT PHASE:
 *   public static function send(string $toEmail, string $toName, string $subject, string $htmlBody): bool
 */
class EmailSender {}
