<?php
/**
 * WhatsAppSender — sends approved WhatsApp template messages via
 * WHATSAPP_PROVIDER (Meta Cloud API directly, or a BSP like
 * Gupshup/Interakt/AiSensy). Requires WhatsApp Business verification and
 * pre-approved message templates — see docs/NOTIFICATIONS_COMPLIANCE.md.
 *
 * NEXT PHASE:
 *   public static function sendTemplate(string $toPhoneE164, string $templateName, array $params): bool
 */
class WhatsAppSender {}
